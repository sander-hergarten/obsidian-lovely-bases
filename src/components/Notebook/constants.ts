import type { PageStyle } from "./types";

export const DEFAULTS = {
  width: 128,
  aspectRatio: 7 / 10,
  padContent: false,
  pageStyle: "plain" as PageStyle,
  showCounter: false,
  previewFilesAmount: 5,
}
